---
title: Tasks
part_of: '[provui](/README.md)'
contents:
- '[The metadata axes say they rewrite every document, and nothing does](/docs/tasks/metadata-axes-claim-a-rewrite.md)'
---

# Tasks

Work this repository has committed to and deferred.
One file per task, named by a slug; `status:` in its frontmatter is what `dx tasks` reads.
`contents:` above lists every task, open and closed — the index is the spine, and what is open is a view of it (`dx tasks`); a task that reaches `done` or `dropped` keeps its file and its place here.

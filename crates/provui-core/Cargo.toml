[package]
name = "provui-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
readme.workspace = true
license.workspace = true
description = "A frontend-neutral UI composition core over prov: a prov-aware structural metadata editor (flower over prov), a document session composing flower metadata and leaf body over one prov document, and the prov-config → flower-schema adapters."
keywords = ["prov", "editor", "metadata", "frontmatter", "schema"]
categories = ["text-editors", "config"]

[dependencies]
prov.workspace = true
fig.workspace = true
flower-core.workspace = true
leaf-core.workspace = true
